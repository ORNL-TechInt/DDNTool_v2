# This file exists to work around what I think is a bug in the python distutils that ship
# with RHEL 6.  Once we no longer need to support RHEL6, we can (hopefully) remove this file.
#
# UPDATE: Nope.  We still need this file in CentOS7.  Need to figure out why and fix it!
include src/init/DDNTool.conf src/ddntool.conf.sample src/init/DDNTool.service
